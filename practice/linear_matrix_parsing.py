from typing import List


def liner_matrix_parser(matrix: List[List[int]]) -> None:
    loop_counter = i = j = 0
    row = len(matrix)
    column = len(matrix[0])

    while loop_counter < row * column:
        pass
